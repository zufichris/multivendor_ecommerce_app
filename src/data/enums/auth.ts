export enum AuthTypes {
  OAuth = "OAuth2.0",
  EmailAndPassword = "EmailAndPassword",
}

export enum OAuthProviders {
  Google = "Google",
  Github = "Github",
  Twitter = "Twitter",
  Apple = "Apple",
}
